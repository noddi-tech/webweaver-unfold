

# Update "Trusted by" Label Text

Single-line change in `src/components/LogoMarquee.tsx` — update the fallback text in the `TrustedLabel` component from "Trusted by leading Nordic service providers" to "Trusted by leading service providers". The translation key `hero.trusted_by` stays the same.

**File**: `src/components/LogoMarquee.tsx`  
**Change**: Update the fallback string in the `t()` call inside `TrustedLabel`.

